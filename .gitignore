# toolchain and emulator binaries are repo-local but never committed;
# see README.md for where to get them.
/cc65/
/emulator/

# build products
/build/
/sdcard/
*.o
*.PRG
*.prg
*.map

# scratch
/test/fsroot/
# a standalone cart run (paint.bat, no -fsroot) writes the app's data file
# into the repo root -- e.g. paint's canvas save
/PAINT.DAT

# local project brief for Claude -- kept local, never published
/CLAUDE.md

# python bytecode
__pycache__/
